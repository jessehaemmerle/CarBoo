# Traefik Standalone Configuration
# Configure this for your main domain

# Main domain for Traefik dashboard
MAIN_DOMAIN=yourdomain.com

# Email for Let's Encrypt certificates (required)
ACME_EMAIL=admin@yourdomain.com

# Optional: Traefik dashboard authentication
# Generate with: echo $(htpasswd -nb admin your-password) | sed -e s/\\$/\\$\\$/g
# Or use online generator: https://hostingcanada.org/htpasswd-generator/
TRAEFIK_AUTH=admin:$$2y$$10$$placeholder-replace-with-real-hash